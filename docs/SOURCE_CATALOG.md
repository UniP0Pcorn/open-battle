# 外部规则资料目录

用户提供的 PDF 资料盘点见 [warhammer_pdf_catalog.md](../../outputs/warhammer_pdf_catalog.md)。项目只记录来源目录和版本信息，不把 PDF 正文或整套兵表复制进仓库。

导入器应接收用户有权使用的本地数据包，并要求每个数据包声明 `edition`、`source_version`、`updated_at` 和 `license_note`。10 版与 11 版必须使用不同命名空间，避免点数、关键词和规则条件交叉污染。
